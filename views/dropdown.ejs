<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <select id="creatorDropdown">
    <!-- データをドロップダウンリストに追加 -->
        <% mouses.forEach(item => { %>
            <option value="<%= item.creator %>"><%= item.creator %></option>
        <% }); %>
    </select>

    
</body>
</html>